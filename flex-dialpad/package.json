{
  "name": "flex-dialpad",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^0.27.2",
    "twilio-flex-token-validator": "^1.5.1"
  },
  "scripts": {
    "build": "cd ui-src && twilio flex:plugins:build && cp build/** ../assets"
  }
}
