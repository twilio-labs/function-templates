{
  "name": "twilio-keyword",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "@analytics/segment": "^1.1.0",
    "airtable": "^0.11.0",
    "analytics": "^0.7.5",
    "axios": "^0.21.1",
    "twilio": "^3.62.0"
  }
}
